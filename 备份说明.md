# 备份 039：预读失败恢复升级前基线

## 基线

- `备份/039` 完整保存 Version2 `1.1.6`、Provider 设置 v5、human-reading-v4 的源码、测试、文档和构建材料，不含 node_modules、dist、release 及其他备份。
- 已包含安全暂停、分片断点、输出截断自适应二分、整章实体覆盖以及未命名泛称排除。
- 修改前，Provider 只重试网络、服务器、限流、超时和空响应；章节级无效 JSON、最终截断或整章覆盖修复失败会直接把章节标为失败。

## 039 与当前 1.1.7 的差异

### 有上限的章节级自动恢复

- 网络、网关、限流和超时在请求自身重试耗尽后，再进行一次章节级断点恢复。
- 截断、空响应、无效响应、模型 JSON 解析失败和整章实体覆盖修复失败最多执行三次章节尝试。
- 身份验证、权限、模型不存在、错误请求、用户取消及未知程序错误不会盲目重试。
- 自动恢复只把当前章节重新排队，不清空 `checkpoint_json`；再次领取同一章节时从已完成分片之后继续。
- 用户手动点击重试时保留章节断点，但重置该失败项的尝试计数，使其重新获得完整自动恢复次数。

### 更细截断拆分与原子提交

- 截断自适应安全下限由 500 字降至 240 字，300～500 字的高输出密度分片也可继续二分。
- 章节仍在全部分片、泛称清理和实体覆盖校验通过后才一次性提交正式术语、事实、事件和长期记忆。
- 最终失败时日志明确列出断点内已完成分片及人物、术语、事实、事件数量，并说明它们尚未正式提交，而不是含糊显示“没写入”。
- 可选独立复核警告继续采用成功的初次提取，不因非关键复核失败重复生成或丢弃内容。

### 失败任务可见与可续跑

- 工作台顶部把最新失败任务纳入进度展示，直接提供“从断点重试失败章节”。
- 作品主页五步向导和任务中心同样显示失败章数、断点保留状态和重试入口。
- 最新失败任务未处理前会阻止另开冲突工作流，避免用户只能跳过预读直接翻译。

## 修改文件

- 新增恢复策略：`electron/workflow/preReadRecovery.cts` 及测试。
- 工作流与持久化：`electron/workflow/workflowService.cts`、`workflowRepository.cts` 及仓储回归测试。
- 界面：`useWorkflowOverview.ts`、`TaskCenter.tsx`、`OverviewPanel.tsx`、`TranslationWorkbench.tsx`、`workshop.css`。
- 发布：版本号、README、便携包使用说明、TODOLIST 和本备份差异记录。

## 验证

- 全量回归：39 个测试文件、169 项测试通过。
- Renderer TypeScript、Vite 生产构建、Electron TypeScript 与主进程打包通过。
- 新测试覆盖可恢复/不可恢复错误分类、低于旧 500 字下限的二次拆分、断点内知识计数、自动重新排队不清检查点，以及手动重试恢复完整尝试次数。
- 未读取、复制或记录 API Key；真实第三方网关的断点恢复仍需在 1.1.7 便携版中继续失败章节验证。

---

# 备份 038：暂停与分片级实体检测调整前基线

## 基线与触发问题

- `备份/038` 完整保存 Version2 `1.1.5`、Provider 设置 v5、human-reading-v4 的源码、测试、文档和构建材料，不含 node_modules、dist、release 及其他备份。
- 用户在第 2 章分片复核期间点击暂停。当前请求虽已取消，但可选复核的 catch 把 `cancelled` 当作普通复核失败吞掉，随后分片级实体覆盖逻辑又建立了新的修复请求，因此表现为“有时无法暂停”。
- 旧版每完成一个分片就检查实体引用。前面分片尚未读到后续才揭示的姓名、别名或专名时，会过早触发补建；日志中的“敵、兵士”等词是未解析引用，不等于已经写入术语表，但模型修复若误建普通词仍可能污染词表。

## 038 与当前 1.1.6 的差异

- 每次模型请求开始前和响应返回后都检查任务停止意图；暂停会阻止新请求越过安全断点。分片独立复核收到取消时立即向上退出，不再记录成可忽略的复核失败。
- 实体覆盖检查从“每个分片后”移动到“全部分片提取、复核并写入章节断点后”。前面分片的引用可以由本章后续分片或前章已持久化的规范名、别名与术语解析。
- 整章仍有缺口时只调用一次紧凑补丁修复，模型只返回新增人物、术语和 Key 改写，不再重复输出整章 JSON，降低长章节再次触发输出截断的风险。
- `敵、兵士、敵軍、相手、仲間、男、女` 等未命名泛称由本地确定性规则清空持久 Key，但原事实 statement 不变；这些词既不会成为人物实体，也不会计入或写入术语表。
- 日志会明确区分“清理泛称”和“整章实体/专名统一”，并显示新增人物、术语、引用改写及剩余缺口数量。

## 预期效果

- 暂停后不会出现新的实体修复、翻译或复核请求；最近完成的分片断点仍可继续。
- “イルドリア戦線、帝国”等真正专名可在整章统一时建立或复用；“敵、兵士”等普通泛称保留语义叙述但不污染术语表。
- 后续分片出现完整人名时可以自然补足前面分片，无需为每个分片重复输出同一人物实体。

## 验证

- 全量回归：38 个测试文件、166 项测试通过。
- 新增暂停发生在可选分片复核期间的回归，确认取消后请求总数不再增加且任务进入 paused。
- 新增跨分片解析、前章已知 Key、泛称清理、紧凑补丁全路径改写和术语计数测试。
- Renderer TypeScript、Vite 生产构建、Electron TypeScript、主进程打包和 Windows x64 便携包构建通过。
- 未读取、复制或记录 API Key；真实在途任务需要用户在 1.1.6 正式便携版中继续验证。

---

# 备份 037：移除 Command Code 显式思考前基线

## 基线与触发问题

- `备份/037` 完整保存 Version2 `1.1.4`、Provider 设置 v4、human-reading-v4 的源码、测试、文档和构建材料。
- 用户实测开启 Command Code 思考设置后，Command Code 中的 DeepSeek 持续无法使用。
- 代码核对确认：旧版会向 Command Code DeepSeek 发送 `thinking` 与 `reasoning_effort`，失败后还会执行 Max→High 等降档、无参数回退及能力缓存。一个业务请求可能在真正兼容调用前先发生多次确定性失败。

## 037 与当前 1.1.5 的差异

- Command Code 的 Chat、Messages 与各工位请求均不再发送 `thinking`、`reasoning_effort` 或 `reasoning`。
- 删除 Command Code 专属思考能力探测、档位映射、失败降档、能力缓存及相关诊断；保留模型自动协议分流、ZDR、JSON 参数拒绝回退和截断自愈。
- Provider 设置升级至 v5。所有内置及自建 Command Code 配置在加载和保存时强制迁移为 `reasoningEffort=none`、`agentReasoningEffort=none`，旧能力探测记录从普通设置文件中清除。
- 设置页对 Command Code 隐藏两组思考下拉框，明确显示“由上游模型默认决定”；切换服务类型为 Command Code 时也不会再把思考档位改成 High。
- DeepSeek 官方继续使用其专属 thinking 参数，Kouri 继续保留 Responses 能力探测与兼容回退，两者不受此次修改影响。

## 预期效果

- Command Code DeepSeek 每次业务调用不再为了探测思考档位先失败若干次，避免 JSON/网关参数不兼容造成“始终无法使用”。
- “不发送显式思考参数”不等于强迫模型立即回答；上游若默认启用内部推理，仍由 Command Code 和目标模型自行执行，只是软件不再干预。

## 验证

- 全量回归：38 个测试文件、161 项测试通过。
- 定向验证 Command Code 即使旧配置保存了 High/Max，也不会向请求体发送 `thinking`、`reasoning_effort` 或 `reasoning`；旧能力缓存会在设置迁移时删除。
- Renderer TypeScript、Vite 生产构建、Electron TypeScript、主进程打包和 Windows x64 便携包构建通过。
- 未读取、复制或记录 API Key；真实 Command Code GOAT 账户需由用户在 1.1.5 正式便携版中执行连接测试及续跑任务。

---

# 备份 036：人物实体与术语覆盖修复前基线

## 基线与触发问题

- `备份/036` 完整保存 Version2 `1.1.3`、SQLite schema v9、human-reading-v3 的源码、测试、设计文档和构建材料，不含 node_modules、dist、release 及其他备份。
- 用户使用 Command Code GOAT 的 `google/gemini-3.7-flash` 成功完成第 2 章预读；模型返回 10 条状态事实、5 条定向事件、8 条场景框架和 10 条文风决策，但 `entities=[]`、`glossary=[]`。
- 实际 SQLite 数据证明章节摘要和事件已识别“谭雅·提古雷查夫 / ターニャ・デグレチャフ”，事实键却使用自造的 `tanya_degurechaff`。人物实体表、别名表和术语表均为空。
- 旧提示只完整展示 facts/frames 等结构，却把 entities/glossary 留作空数组示意；独立复核也没有把“命名人物缺实体”列为拒绝条件。
- 叙事持久层只能用逐字日文规范名或别名解析人物。自造 slug 无法绑定实体，导致已经标记 verified 的 A→B 事件入库时降为 unresolved，因此不是单纯的页面计数问题。

## 036 与当前 1.1.4 的差异

- 预读协议升级为 `human-reading-v4`，完整规定 entities、aliases、attributes 和 glossary 的 JSON 字段、证据与时间位置。
- 主角、视角人物、说话人、命名参与者首次出现明确证据时必须建立人物实体；人物实体自动进入术语表，避免重复建词。
- 所有事实、事件、角色知识、场景和人物文风中的非空引用键必须逐字匹配日文规范名、别名或术语，不允许英文 slug、中文译名和泛称充当持久引用键。
- 新增本地实体覆盖审计。模型复核即使误判 pass，只要存在未解析键，就会触发一次专门的实体修复调用；修复后仍不完整则阻止该章入库并明确报错，不再产生伪成功。
- 运行日志把原来仅统计 glossary 的“提取术语”改为去重后的“人物/术语”，并单列人物实体数量。
- 暂停任务若来自旧预读协议，继续时会清空不兼容的旧分片检查点并重新核对所有章节；任务本身与累计 Token 用量保留，确保谭雅所在的已完成章节也能被纠正。
- 实体覆盖检查独立为 `preReadCoverage.cts`，没有继续把逻辑堆入工作流主文件；测试覆盖缺失人物、自造 slug、规范名/别名解析、无证据实体及术语去重计数。

## 预期效果

- “谭雅”这类主角会在首次有全名证据的分片进入人物实体和术语表；后续姓、名、军衔、代号、昵称可作为有时间与证据的别名合并。
- 谭雅发出或承受的动作、对话和角色知识使用可解析日文键，能实际绑定同一人物，A→B 不再因数据库找不到实体而静默降级。
- 封面、版权页等确实没有作品实体的章节仍允许人物/术语为 0；“0”本身不是错误，命名引用存在却无法绑定才是错误。

## 验证

- 全量回归：38 个测试文件、164 项测试通过。
- Renderer TypeScript、Vite 生产构建、Electron TypeScript 与主进程打包通过。
- 定向覆盖谭雅式自造 slug/缺失人物、无证据实体、规范名与别名绑定、人物/术语去重计数，以及旧提示版本任务的全项重核与检查点清理。
- 未读取、复制或记录 API Key；真实 Gemini 3.7 Flash 的实体补建结果需在 1.1.4 便携版中继续当前任务验证。

---

# 备份 035：请求可观测性、截断自愈与 18432 上限升级前基线

## 基线

- 保存 Version2 `1.1.2`、SQLite schema v9、human-reading-v3 的完整源码、测试、设计文档与构建材料。
- 已包含 Command Code GOAT 双协议分流、DeepSeek thinking/reasoning_effort 能力探测与全书持续认知。
- 修改前，内置 Command Code GOAT 的最大输出为 8192 Token。

## 触发问题

用户实测 Command Code GOAT 全书预读时，第 2 章 1769 字分片从 12:04:03 等待到 12:09:20，随后才显示输出长度上限并二分为约 819 字。DeepSeek 官方和 Kouri 在同一类工作中未触发二分。

源码核对确认：

1. 二分并非 Command Code 专属。任何服务明确返回 `finish_reason=length`、Responses `incomplete` 或 Anthropic `max_tokens` 都会触发。
2. Provider Adapter 错把确定性的 `truncated-response` 列入临时故障重试。同一输入会在工作流看到截断前原样调用最多 `maxRetries + 1` 次；内部重试没有运行日志，因此表现为长时间“卡住”。
3. 运行终端只显示工作流开始与结束，不显示请求编号、服务、协议、思考、实际上限、重试和等待心跳；`details` 与 `model` 虽已保存但界面没有展开。
4. Command Code DeepSeek 的 JSON 任务没有发送 DeepSeek 官方支持的 `response_format: {"type":"json_object"}`，仅依赖提示和本地解析，增加输出游离和膨胀风险。
5. 一次分片截断后，软件只拆当前分片，不把学到的安全长度用于后续分片或关闭后的断点。
6. 预读独立复核失败会静默采用初次提取结果。

## 035 与当前 1.1.3 的差异

### 请求可观测性

- 每个预读、翻译、A→B 语义角色和独立复核调用获得短请求编号。
- 发送时显示服务名、模型、实际协议、思考请求、JSON 处理方式、最大输出 Token、超时、临时重试次数和本次原文字数。
- 每等待 30 秒输出一次心跳，明确区分网关排队、模型生成/传输与软件卡死。
- 限流、服务器/网络/超时重试，Kouri 协议回退，Kouri/Command Code 思考降档，以及 Command Code JSON 降级都进入日志。
- 终端默认显示详情行、模型、输入/输出 Token 和耗时，宽度由 820px 增至 1040px。

### 截断与自适应分片

- `truncated-response` 从通用重试名单移除；确定性的长度截断不再用相同输入盲重试。
- Chat/Responses/Anthropic 的截断错误保留结束原因、输入 Token、输出 Token与协议，日志能直接显示是否确实用满上限。
- 预读只对明确截断启动二分，不把协议 JSON 错误冒充长度问题。
- 首次截断后学习安全字符长度，后续超出该长度的分片先拆再请求。
- 学到的安全长度立即写入分片检查点；关闭或中断后继续任务仍可复用。
- 预读复核失败不再静默，终端说明原因和采用初次提取结果的降级行为。
- 预读提示要求证据最短充分、同项去重、紧凑 JSON，减少无质量收益的输出膨胀，不以固定低上限粗暴丢事实。

### Command Code JSON 与 18432

- 对明确识别为 DeepSeek V4 的 Command Code Chat JSON 请求发送原生 `response_format=json_object`。
- 如果具体上游以 400/422 明确拒绝 `response_format`，自动去掉参数重试一次，并在当前 Adapter 生命周期记住 prompt-only 模式。
- JSON 参数错误与 reasoning 参数错误分开识别，不再把任意“unsupported parameter”误判成思考参数问题。
- Provider 设置格式升级到 v4。内置 `command-code-goat` 且仍为旧默认 8192 的配置迁移到 18432；DeepSeek 官方、Kouri、用户自建值和已经调整过的值不改。
- 设置页说明 18432 能降低截断概率，但可能增加单次耗时与用量；运行终端显示实际发送值。

## 依据与边界

- DeepSeek 官方 JSON Output 文档要求设置 `response_format={"type":"json_object"}` 并在提示中要求 JSON：
  https://api-docs.deepseek.com/guides/json_mode/
- Command Code Provider API 文档说明 `/provider/v1/chat/completions` 遵循 OpenAI Chat Completions 请求/响应格式：
  https://commandcode.ai/docs/provider
- DeepSeek V4 当前官方模型页标注最大输出高于 18432，因此 18432 在模型能力范围内：
  https://api-docs.deepseek.com/quick_start/pricing/
- Command Code 聚合到的具体上游仍可能有额外限制，所以保留明确错误、自动 JSON 降级和长度自愈，不能把模型厂商上限当成网关保证。
- 当前仍使用非流式完整响应。软件可以证明请求仍在等待、已经等了多久以及何时重试/降级，但在服务返回前无法查看供应商内部的逐 Token 思考内容。

## 修改文件

- Provider：`electron/providers/models.cts`、`commandCodeCompatibility.cts`、`providerAdapter.cts`、`providerSettings.cts` 及对应测试。
- 工作流：`electron/workflow/workflowService.cts`、`prompts.cts`。
- 界面：`src/features/console/LiveConsoleDrawer.tsx`、`src/styles/console.css`、`src/features/settings/ProviderSettings.tsx`。
- 发布：版本号、README、便携包使用说明、TODOLIST 和本备份差异记录。

## 验证

- Provider/设置/Command Code 定向测试：26 项通过。
- 全量回归：37 个测试文件、160 项测试通过。
- Renderer TypeScript、Vite 生产构建、Electron TypeScript 与主进程打包通过。
- 新测试覆盖 Command Code 原生 JSON 载荷、JSON 参数拒绝后的单次降级与记忆、长度截断不盲重试及 Token 元数据、v3 内置配置 8192→18432 且不修改官方/Kouri。
- 未读取、复制或记录用户 API Key；真实 Command Code 账户的上游响应仍需用户在正式软件中继续任务验证。
