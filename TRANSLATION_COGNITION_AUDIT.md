# Version2 翻译认知能力审计

最后核验：2026-08-27（1.1.4 / SQLite schema v9 / human-reading-v4）

## 结论

1.1.0 已把上一轮列出的程序升级全部接入运行时：字符位置状态、章节/卷记忆巩固、分层保留、人工明确的跨卷系列记忆、本地句法证据、结构化文风决策、嵌套叙事和歧义工作台。它们不是把更多文本无差别塞进提示词，而是用证据、时间、读者知识和置信度决定什么可以进入哪一条通道。

这些机制有实际作用，并针对常见副作用设置了硬门：候选不能冒充事实；普通细节不能由模型擅自升级为跨卷设定；后文可以帮助译者理解但标记为不得表面化；重复证据无法唯一定位时保持 ambiguous；冲突和人工裁定使旧译文转入复核而不静默覆盖。

这仍不是“所有小说自动等同优秀人类译者”的科学证明。基础模型能力、无表面线索的省略、高难双关与文学判断仍是边界；专业整本盲评仍是最终结论的必要条件。

## 已接入能力

| 能力 | 1.1.0 状态 | 运行时机制 |
| --- | --- | --- |
| 关闭后继续预读 | 已实现 | 分片 checkpoint 持久化；异常关闭后任务变为 interrupted，可继续未完成分片。 |
| 字符位置状态 | 已实现 | evidence/claim/event/alias/knowledge/frame/state 保存 UTF-16 start/end offset；变化点前使用旧状态，变化点后应用新状态。 |
| 重复短句定位 | 安全实现 | 只有“分段＋offset＋原文”匹配或全章唯一命中才为 exact；否则是 ambiguous/unlocated。 |
| 人物、多名、性别、人数、年龄、外貌 | 强类型实现 | 实体、别名和时态状态分表；未知保持 unknown，长期知识不授权在当前原文中增写代词或人数。 |
| 关系、称呼和 A→B | 方向实现 | A→B 与 B→A 是不同轨道；事件保存 agent/patient/recipient，对话保存 speaker/addressee。 |
| 句法交叉检查 | 已实现保守证据层 | 本地识别使役、受身、使役受身、授受、格助词和引语边界；与模型 voice 冲突时降置信度并阻断自动通过。 |
| 嵌套叙事 | 强类型实现 | frame 保存 parent、nesting depth、worldline、story time、direct/indirect/free-indirect/monologue、quote level 和说话关系。 |
| 读者/译者/角色知识 | 字符级隔离 | 当前读者材料、不得表面化的未来材料和角色 knows/believes/suspects/denies 分开检索。 |
| 分层记忆 | 已实现 | canon/character/relationship/event/state/episode-detail 六类，permanent/stable/episodic/working 四种保留策略。 |
| 章节巩固 | 已实现 | 每章结束按证据和置信度巩固；新状态 supersedes 旧状态，历史记录保留；低价值工作记忆按策略归档。 |
| 卷级档案 | 已实现 | 全书预读完成后生成卷档案；档案在全书末尾才成为当前卷读者已知。 |
| 跨卷系列记忆 | 已实现且默认关闭 | 只接受用户明确系列名与卷序；只从更早卷取 confirmed/locked、无冲突且相关的实体、术语、文风和记忆。 |
| 文风决策 | 已实现 | 按系列/叙述者/人物/关系/场景保存 register、称呼、句法、节奏、标点、口癖等，有证据和有效区间。 |
| 歧义工作台 | 已实现 | 双关、身份、指代、作用域、角色方向、语态、时间和叙事层保存多个解释；可保留多解、选择解释、音译、译注或继续复核。 |
| 译文依赖与失效 | 已扩展 | 译文版本记录 memory/style/ambiguity/syntax/series 依赖；知识、文风或歧义变化会保留旧稿并转 needs-human。 |

## 翻译模型实际看到的材料

每个批次使用独立通道，而不是一份混杂“百科摘要”：

1. 当前原文、原译、近邻和已确认前文成稿；
2. 当前精确阅读位置、worldline、scene 和嵌套叙事 frame；
3. 当前可见实体、别名、状态、关系与角色知识；
4. 同段按字符 offset 切分的原文片，以及每个变化点开始的 claim/event/frame/knowledge；
5. 当前可表面化的已巩固记忆；
6. 可帮助理解但 `maySurface=false` 的未来记忆；
7. 用户明确关联后才存在的较早卷系列上下文；
8. 当前有效的文风决策；
9. 多候选歧义与人工保存策略；
10. 本地句法证据和模型语义角色账本；
11. 当前原文实际命中的确认/锁定术语。

所有长期材料都受最高忠实边界约束：它可以防止误解，不能授权增加日文没有的性别、主语、人数、称呼、动作、因果或心理。

## 为什么这次升级不会自然降低质量

风险不是零，但已经针对性封堵：

- **错误记忆固化**：只有确认/锁定、置信度达到阈值且证据精确的记录才能 consolidated；其他保留 candidate/conflict。
- **上下文噪声**：按人物命中、重要度、近期性、场景和数量上限检索；工作/低重要情节会归档。
- **后文剧透**：未来材料与当前材料分开，明确禁止表面化；章节摘要与卷档案只在对应文本末尾成为读者已知。
- **同段倒灌**：段首状态查询以 offset 0 为边界，后半段状态只进入 future 通道；精确切片告诉模型从哪个字符开始变化。
- **跨卷串书**：软件不按书名猜系列；只有人工关联，并且只允许较早卷流向较晚卷。
- **模型自封“设定”**：记忆类别和最大检索范围由确定性策略决定；模型只能小幅调整重要度或缩小范围。
- **歧义被强行解开**：候选解释单独保存，不进入事实；preserve 策略明确禁止模型选边。
- **旧译文被悄悄改写**：任何依赖变化只创建 blocking 复核并标记 needs-human，旧版本仍可恢复。

## GraphRAG 的位置

GraphRAG 不是当前硬约束核心，也不是实现这些能力的前置条件。SQLite v9 的时态事实图已经能精确表达实体、方向、时间、世界线、知识边界、证据和译文依赖；这些是向量相似或社区摘要不能单独保证的。

如果未来处理超大系列，可以把 GraphRAG/向量索引用作“候选召回层”，帮助发现可能相关的人物、关系和主题。但召回结果必须回到 evidence/entity/claim/event/frame/knowledge/ambiguity 表验证，未经验证不能直接进入翻译上下文，更不能覆盖确定性时间与方向约束。

## 仍不能夸大的边界

- 本地句法层是保守的形态、助词和引语证据，不是完整日语依存句法或形式证明。
- 没有表面线索的省略主语、作者故意让多个解释长期共存、高难双关和极端嵌套叙事仍可能需要人工。
- 双模型一致不等于真值；高风险冲突必须进入复核。
- 143 项自动测试证明数据边界、迁移、检索和工作流行为，不证明文学质量已经达到专业译者。
- 未完成专业日中译者整本盲评，因此不能宣称所有目标语料已经匹敌优秀人工译者。

## 老项目升级要求

数据库会在 schema 升级前建立一致性快照并迁移到 v9，旧原书、术语、事实、任务、译文和版本不会丢失。但旧预读没有 v3 的字符证据、分层记忆、文风、歧义和嵌套 frame，不能冒充 v3 前置条件。升级后需要重新执行一次全书预读，再开始新翻译。

## Kouri 实测状态

本机保存的 Kouri 配置 `deepseek-v4-flash-0731` 已于 2026-08-27 通过真实 Responses 请求验证：普通连接、结构化 JSON、`reasoning: { effort: "low" }` 和 `reasoning: { effort: "high" }` 均成功。能力记录按服务配置、模型与协议持久化；若其他模型拒绝某档位，软件逐级降档，全部拒绝才回退模型默认推理。因此 Kouri 的 DeepSeek 并非被永久限制为“立即回答版”。
