# Version 2：章节阅读、中文校改与 EPUB 校样导出

最后更新：2026-08-26

## 1. 本阶段已经完成什么

010 在 009 的不可变 EPUB 源文层上建立了第一条可见、可保存、可往返的编辑闭环：

1. 按 OPF spine 顺序列出全部阅读项；默认跳过只有封面或结构、没有正文块的项目。
2. 分页读取 EPUB 文本块或 TXT 段落，不把 XHTML、CSS、script 或外部页面送入 Renderer。
3. 保存每本作品最后阅读的章节和段落页位置，关闭后继续。
4. 对已识别日中双语 EPUB，只允许人工修改现有的普通中文段；日文原段保持只读。
5. 草稿单独写入 SQLite，不修改不可变原包和源文本块。
6. 从原包快照生成新的“校样 EPUB”，写回前重新核对文档摘要、DOM 路径、节点 XML 和草稿所依据的源摘要。
7. 输出后重新检查 ZIP、mimetype、资源清单、所有未修改资源内容，并重新解析 container、OPF、spine、NAV/NCX 和 XHTML。

这不是 DeepSeek 翻译功能，也不是最终成品导出。它先证明以后由模型产生的中文能够落到可追溯节点，并且不会依靠全书字符串替换污染原文。

## 2. 界面行为

左侧“章节与正文”已经开放：

- 阅读顺序保留长章节名并可滚动；当前章节会自动进入可见范围。
- 正文使用纯文本阅读视图，保留日文、中文、ruby 的可读表示和源段编号，但不执行书内活动内容。
- 双语块显示“已有中文 / 日文原文”和配对序号。
- 普通中文段提供“校改”；保存后显示原中文与校改稿，允许继续修改或恢复原中文。
- 导出按钮只有在 EPUB 至少保存一段校改后可用；系统另存窗口禁止覆盖导入时的原文件。
- TXT 当前只读。日文 EPUB 也只读，避免把人工输入误当作已经完成的直译管线。

界面继续遵守编辑器型长文本规则：作品名和路径单独换行、状态标签保持原子尺寸、正文不使用省略号、720×680 与 140% 字号时章节目录改为上方滚动区。

## 3. SQLite v3

在 v2 源文表之外新增：

- `reading_positions`：每个项目最后阅读的章节、块序号和更新时间。
- `epub_block_drafts`：块 ID、中文校改、保存时源摘要和更新时间。

草稿使用外键绑定 `epub_text_blocks`，删除项目或源块时自动删除；原始 `source_archives`、`source_xml` 和 `source_text` 不被覆盖。v1 TXT 数据库会顺序迁移 v1 -> v2 -> v3；v2 EPUB 数据库直接迁移到 v3。

## 4. 首版可写与受保护范围

当前允许写回的块必须同时满足：

- 项目已经由结构证据识别为日中双语；
- 当前块是已有中文，而不是日文或不明结构文字；
- 块元素内部没有子元素；
- 草稿非空、不超过 20 万字符且不含 XML 禁止码点。

含 ruby、链接、强调、脚注锚、行内图片或其他子元素的块保持只读。这样做是有意的质量门：010 不会用 `textContent` 覆盖 children，也不会把复杂 XHTML 扁平化。后续必须完成 `tokenized-block` 的不透明标记回环后才能开放复杂块。

## 5. 精确写回与打包

写回从数据库中的原 EPUB BLOB 派生，不重新读取或修改用户原文件：

1. 核对原包 SHA-256。
2. 按文档分组，核对每个 XHTML 的原始字节摘要。
3. 重新用稳定 DOM 路径定位元素，并核对序列化 XML 与节点摘要。
4. 使用非执行型 XML DOM 确认元素没有子元素。
5. 使用独立词法定位器找到原 XHTML 中该元素的精确内容区间，只转义并替换文本内容；元素标签、属性和文档其余字节不经 DOM 重序列化。
6. 保留原 XML 编码与 BOM，按原条目 STORE/DEFLATE 方式写入修改文档。
7. 强制标准 `mimetype` 第一项且 STORE；资源集合必须与原包一致。
8. 对全部未修改资源比较解压后字节；任何差异阻断导出。
9. 对输出运行同一套 ZIP 安全门和 EPUB 结构解析器。

ZIP 中除 `mimetype` 外的物理条目次序不是 OPF 阅读顺序，JSZip 可能调整它；校验关注资源集合、内容和 OPF spine，不把无语义的 ZIP 条目重排当作正文结构变化。

## 6. 真实样书结果

- SCJP《病弱少女》：4,528 个中文块全部符合首版安全写回条件；测试改动 1 块、1 个 XHTML，输出重新导入后仍为 38 个 spine 项、9,232 个文本块。
- Polished SC&JP《幼女戦記-Web》：40,293 个中文块全部符合条件；测试改动 1 块、1 个 XHTML，输出重新导入后仍为 127 个 spine 项、83,099 个文本块。
- 两次导出均逐项验证未修改资源内容不变。
- 009 的 17.35 MiB v2 实测库副本迁移为 v3 后，1 个项目、38 个 spine 项、9,232 个块全部保留，`integrity_check=ok`。

结构测试生成的 EPUB 带“结构往返验证”字样，只存在 `artifacts` 验收目录，不复制到源码备份，也不修改用户提供的四本原文件。

## 7. 尚未完成

- tokenized-block、ruby base、链接、强调和脚注等复杂行内结构写回。
- 新增中文节点、纯中文模式和正式日中双语版式编排。
- EPUBCheck、阅读引擎自动打开、目录/脚注跳转和截图回归。
- 翻译版本、审校版本、批准状态和“失败译文不得导出”的正式质量门。
- DeepSeek Provider、可取消任务、全书预读、事实记忆、术语提取与正式翻译。

因此 010 的按钮明确叫“导出 EPUB 校样”，不能把它作为最终翻译成品导出使用。
