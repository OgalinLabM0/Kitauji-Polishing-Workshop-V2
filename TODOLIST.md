# 北宇治润色工坊 Version 2：正式版清单

当前正式基线：`1.1.7 / 039`  
规则：代码、自动测试、真实界面与正式包实际接通后才能勾选；设计稿或空页面不算完成。

## 1. 正式使用主流程

- [x] 保留京吹风格起始页动画，进入独立书架。
- [x] 原生文件窗口直接导入 EPUB/TXT；相同原书按摘要打开既有项目。
- [x] 书架搜索、排序、最近打开、删除单书、强确认清空。
- [x] 删除项目不删除磁盘原书；缓存清理不删除项目事实或草稿。
- [x] 作品主页、翻译工作台、正文校读、作品阅读、术语、人物、记忆、复核、导出和设置均为独立页面。
- [x] 工作台三栏显示日文、既有中文、当前成稿，支持人工编辑并保存、版本历史和恢复。
- [x] 阅读页支持日文/中文/对照、主题、字号、宽度、亮度和章节切换。
- [x] 正文、表单和操作区增大默认字号；最左侧主导航保持稳定尺度。
- [x] 窗口自由调整；720×600 下重排，长标题、路径和证据完整换行。

## 2. 模型接口与密钥

- [x] DeepSeek 官方配置，默认 Base URL 与 `deepseek-v4-flash`。
- [x] Kouri 配置；根域名自动规范为 `/v1`。
- [x] Kouri 兼容探测：保守请求载荷，Chat/Responses 自动回退；连接测试保存实际成功协议。
- [x] Command Code GOAT 内置配置、官方 Base URL、现有用户配置自动迁移与 Studio API Key 接入。
- [x] Command Code 按实际模型逐请求自动分流：Claude 使用 Anthropic Messages，其余模型使用 OpenAI Chat；支持同一配置四工位混用。
- [x] Command Code 可选 ZDR 请求头、GOAT/Go 权限提示、ZDR 无上游提示和套餐模型列表校验。
- [x] Command Code 所有模型均不发送 thinking/reasoning_effort/reasoning，不执行思考探测与降档；Claude/Chat 协议分流、JSON 兼容回退、ZDR 和截断自愈保持独立。
- [x] 通用 OpenAI-compatible HTTPS 配置。
- [x] Chat Completions 与 Responses 使用各自端点、请求体和返回解析。
- [x] 分别设置翻译、全书预读和独立复核模型。
- [x] 从服务读取模型列表；连接测试明确产生真实调用。
- [x] 批量、并发、超时、重试、输出 Token、温度和推理强度范围校验。
- [x] 429、5xx、网络和超时指数退避；认证、权限、错误请求等不盲目重试。
- [x] 明确的长度截断不再原样重试；Command Code DeepSeek JSON 原生约束被拒绝时自动降级，内置输出上限迁移为 18432。
- [x] 空响应、截断响应、错误协议和模型不存在分类提示。
- [x] API Key 使用 Windows 系统加密保存，完整密钥不回显、不进入项目/日志/缓存/导出。
- [x] 在途请求支持取消，连接测试与任务均使用超时边界。
- [ ] Gemini 原生协议；当前可用范围是 OpenAI-compatible 网关，不把兼容网关冒充官方 Gemini。

## 3. 全书预读与持续认知

- [x] 全书预读按章创建可暂停、继续、取消和崩溃恢复的持久任务。
- [x] 大章分块，块内保留前文，跨章携带滚动章节摘要、已建立事实和术语。
- [x] 提取人物、动物、地点、组织、物品、事件、关系、称呼、声音、场景、伏笔、秘密、谐音和双关。
- [x] 术语记录原文、译名、读音、类型、性别、人数、sense、置信度和日中证据。
- [x] 性别与人数只在有证据时确认；地点/组织等标为不适用，未知不靠名字猜测。
- [x] 人物关系按 A→B 方向、章节阶段和证据保存，不做永久静态标签。
- [x] 区分译者全书知识、当前读者可见知识和角色当前知识。
- [x] 后文知识可辅助消歧，但不能提前泄露或越过角色知识边界。
- [x] 状态、关系、称呼、别名、读者揭示点和角色知识点精确到章节＋分段；同章前后变化不再共用章级状态。
- [x] 场景框架区分现实主线、回忆、预叙、梦境、假设、剧中剧、不可靠叙述和世界线；状态按世界线分别归约。
- [x] 同一分段内的证据、状态、别名、知识与叙事层精确到 UTF-16 字符 offset；重复短句没有唯一位置时保持 ambiguous，不伪造精确边界。
- [x] context frame 支持父子层级、嵌套深度、直接/间接引语、自由间接引语、内心独白、quote level、说话人与受话人。
- [x] 每章自动巩固记忆，按 canon/character/relationship/event/state/episode-detail 分层，使用 permanent/stable/episodic/working 保留策略和 supersedes 历史链。
- [x] 全书预读完成后生成卷级档案；用户明确关联系列和卷序后，仅从更早卷继承已确认、无冲突记忆。
- [x] 文风决策按叙述者/人物/关系/场景、证据和时间范围保存；多解、双关、指代、动作方向和叙事层进入独立歧义库。
- [x] 独立预读复核检查名字猜性别、普通词误收、关系方向、知识边界和谐音遗漏。
- [x] 人物与关系页、证据与记忆页读取作品事实库，不用演示数据冒充。

## 4. 术语与注释

- [x] 全书预读自动生成候选术语并保存证据。
- [x] 术语搜索、状态、类型、性别、人数、读音、sense、置信度和原文/译文语境。
- [x] 逐条一一对应输入，TXT/TSV/CSV/JSON 导入格式和预览。
- [x] 同词异译冲突阻断；用户可确认、锁定、驳回或编辑。
- [x] 已确认术语按当前段原文实际命中注入上下文，不做全书字符串替换。
- [x] 戏称、误读、姓名谐音和双关保留为有证据的情境事实，不被标准译名抹平。
- [x] 人工审核同时显示日文、对应中文、章节和近邻，不要求只看孤词。
- [x] 只有用户明确填写的必要说明才能进入成品 EPUB 注释；AI 内部备注不会自动写入。
- [x] 注释在术语首次正文命中处写为 EPUB 3 noteref/footnote，并提供返回正文链接。

## 5. 翻译、润色与质量门

- [x] 纯日文 EPUB、日文 TXT 生成中文；日中双语 EPUB 以日文为边界润色既有中文。
- [x] 只有全书预读完成后才能开始翻译，避免无长期认知直接批跑。
- [x] 每批获得当前原文、原译文、近邻、滚动摘要、实际命中术语和相关长期事实。
- [x] 批次遇到状态/知识/关系/场景边界自动拆分；同一作品按阅读顺序翻译，禁止后段并发抢跑。
- [x] 当前段额外获得段内变化证据，变化前后按原文证据顺序应用。
- [x] 翻译材料包含段内精确切片、当前/未来分层记忆、跨卷记忆、文风决策、歧义策略、嵌套叙事和本地日语形态/格助词证据。
- [x] 本地句法证据与模型语义角色交叉裁决；强形态与 voice 冲突时置信度自动降至人工复核阈值。
- [x] 内置最高忠实提示：禁止增删、性别/人数/称呼新增、净化、弱化、美化、回避和反向加码。
- [x] 称呼依据当前原文、关系阶段、场景正式度与意图，不机械全书统一。
- [x] 确定性检查覆盖空稿、污染前言、数字事实、明显性别/人数新增和结构错误。
- [x] 硬规则失败反馈给主模型限次修复；修复稿重新通过全部硬规则。
- [x] 独立模型做语义复核；复核修订稿也必须重新通过硬规则，不存在“判官特赦”。
- [x] 批次 JSON/ID 灾难二分拆分，单段失败隔离，不让整章丢失。
- [x] 失败项使任务明确失败，不以“已完成”掩盖部分失败。
- [x] 每段保存模型、提示版本、上下文清单、耗时、Token 和多阶段译文版本。
- [x] 人工修改进入新版本；恢复旧版本前重新执行硬规则。

## 6. 复核与最小人工

- [x] 复核队列显示类别、严重度、原文、原译文、当前稿、候选修订和前后段语境。
- [x] 程序硬错误、独立复核、术语/知识风险进入统一队列。
- [x] 同一问题更新既有未决项，避免重复堆积。
- [x] 采用有效新版本后自动关闭已被替代的旧问题。
- [x] 接受候选、驳回和人工编辑均写入操作记录，不静默覆盖。
- [x] 高置信度常规结果自动通过；多解、身份边界、谐音双关和反复分歧才强制人工。
- [x] 任何未决复核都会阻止正式 EPUB 导出。
- [ ] 用专业日中译者金标完成整本盲评；这是“匹敌优秀人工译者”结论的必要条件，不能用程序测试代替。

## 7. EPUB/TXT 导入与正式导出

- [x] TXT 支持 UTF-8、UTF-16LE/BE、Shift_JIS，拒绝空文件、二进制、失真解码和超限文件。
- [x] EPUB ZIP 路径穿越、重复路径、加密项、异常压缩、container、OPF、spine、NAV/NCX 和 XHTML 检查。
- [x] ruby、图片、竖排、两种日中交替结构与普通行内链接识别。
- [x] script、事件属性和外链只记录、不执行、不联网加载。
- [x] 原始字节、文档摘要、DOM 路径、源 XML 和源译映射持久保存。
- [x] 正式导出支持 JP→SC、SC→JP 和纯中文。
- [x] TXT 项目生成带目录、元数据、样式和注释的 EPUB 3。
- [x] EPUB 项目从不可变原包重建，保留封面、插图、CSS、ruby、强调、链接、脚注、锚点和未修改资源。
- [x] 复杂行内文本按原 DOM 文本槽分配，rt/rp/script/style/svg/math 不被译文覆盖。
- [x] 纯中文模式更新语言元数据与可识别目录标题。
- [x] 输出重新做 ZIP/EPUB 结构解析；原有资源集合变化会阻止导出。
- [x] 成稿不完整、失败或有未决复核时禁止正式导出。
- [x] 保存位置由用户选择；先写唯一临时文件，再替换同名目标。
- [ ] 集成外部 EPUBCheck Java 验证器；当前正式门是软件内置结构、资源和重解析验证。
- [ ] 向任意用户选择的模板 EPUB 注入译文；当前导出以该项目原书快照为模板。

## 8. 数据、恢复与界面

- [x] SQLite v1→v9 增量迁移，外键、完整性、预读断点、字符知识边界、嵌套叙事、分层/跨卷记忆、文风和歧义表测试。
- [x] 规范实体、多重别名、时态 claim、agent/patient/recipient 事件、角色知识、世界状态归约和冲突表接入桌面运行时。
- [x] 翻译前当前句 agent/patient/recipient 与 speaker/addressee 分析、翻译/复核 A→B 双重检查、译文依赖记录和知识变更安全失效。
- [x] 项目、原文、EPUB 结构、阅读位置、缓存、术语证据、记忆、任务、复核、版本和操作日志持久化。
- [x] 渲染界面只走受限 IPC，无任意文件系统、数据库或密钥读取权限。
- [x] 在途任务启动时恢复为 interrupted，可从任务中心继续，不会假装已经完成。
- [x] 同一作品只允许一个活动工作流，避免并发写坏同一批段落。
- [x] 阅读分页缓存可重建并按项目失效；数据库与原书不是缓存。
- [x] 默认书籍、导出和缓存位置可自定义，完整路径可见。
- [x] 85%–140% 文字比例、快捷键与重启保存。
- [x] 页面采用书架列表、连续编辑面、阅读纸页、证据账簿和复核轨道等不同结构，避免同质卡片墙。
- [x] 记忆页提供人工系列关联、分层保留标记和歧义裁定工作台；长原文证据、offset 和解释完整换行。
- [x] 状态文案中文化；长文件名、章节名、角色名和证据不以省略号隐藏。
- [x] 运行终端显示请求编号、服务、模型、协议、思考、JSON 方式、输出上限、超时、重试、原文字数与 30 秒等待心跳；适配降级、截断和复核失败不再静默。
- [x] 项目数据库可迁移到 D 盘等自定义目录；重启时备份并核对完整性、schema 与项目数，失败回退、同名拒绝覆盖。
- [x] 用户可管理的整库备份与恢复；恢复前自动保留当前库，备份与恢复均校验完整性、schema 和项目数量。
- [x] 数据库 schema 升级前自动快照，快照验证失败则停止迁移。
- [ ] 完整键盘/屏幕阅读器审计与 Windows 100%/125%/150% 系统缩放矩阵。

## 9. 验证与发布

- [x] 169 项自动测试覆盖规则、术语、整章实体覆盖、跨分片/前章实体解析、泛称排除、暂停安全断点、预读章节级自动恢复/断点保留/手动重试次数重置、旧任务认知协议重核、旧记忆回填、字符时态上下文、同段状态切换、嵌套/世界线隔离、A→B 方向、句法冲突、记忆分层、跨卷隔离、歧义/文风、译文依赖、Provider、Kouri reasoning/协议回退、Command Code 双协议/ZDR/无显式思考参数/JSON 降级/截断重试策略/18432 迁移、SQLite 备份/恢复/升级、TXT/EPUB 和正式导出。
- [x] 四本用户真实 EPUB 做结构导入核对；两本大型双语 EPUB 做校样往返。
- [x] 正式工作区以真实双语样书数据库做 1080p 界面核对，不使用卡片式占位 Demo。
- [x] Windows x64 正式便携版一键构建，包名和 exe 不含“测试版”。
- [x] 正式包内附使用说明、忠实边界、工作顺序、接口配置、数据安全和真实边界。
- [x] `备份/015` 保存数据库位置自定义后的 1.0.1 源码与打包基线，版本记录同步。
- [x] `备份/033` 保存 Command Code Provider 接入前的 1.1.0 源码基线，并记录 1.1.1 的完整差异。
- [x] `备份/034` 保存 Command Code DeepSeek 思考等级启用前的 1.1.1 源码基线，并记录 1.1.2 的完整差异。
- [x] `备份/035` 保存请求可观测性、截断自愈与 18432 输出上限升级前的 1.1.2 源码基线，并记录 1.1.3 的完整差异。
- [x] `备份/036` 保存人物实体覆盖修复前的 1.1.3 源码基线；1.1.4 强制人物/术语与事实、事件、场景引用键可解析，并自动重核旧提示版本的暂停任务。
- [x] `备份/037` 保存移除 Command Code 显式思考前的 1.1.4 源码基线；1.1.5 删除 Command 专属 thinking/reasoning 参数、能力缓存、探测和降档，并将旧 Command 配置迁移为上游默认模式。
- [x] `备份/038` 保存暂停与分片级实体检测调整前的 1.1.5 源码基线；1.1.6 阻止暂停后新请求，把覆盖检查后移到整章，并将未命名泛称排除出人物/术语图谱。
- [x] `备份/039` 保存预读章节级恢复与失败任务入口加入前的 1.1.6 源码基线；1.1.7 对可恢复失败从分片断点自动重试、降低截断二分下限，并让失败章节可见且可手动续跑。
- [ ] 恶意 EPUB 专项语料、大型损坏书、断网/限流/磁盘不足/强杀恢复的扩展矩阵。
- [ ] 专业译者盲评达到预设门槛后，才可宣称在目标语料上匹敌优秀人工译者。

## 10. 不冒充完成的后续项

以下项目不阻塞当前主流程，但界面与说明不得声称已有：Gemini 原生协议、外部 EPUBCheck、任意模板注入、全局替换/错位修复等旧版高级编辑工具、完整传统日语句法解析器的形式证明、专业译者盲评结论。当前本地句法层是保守的形态/助词/引语证据交叉检查，不冒充完整句法证明。后续完成一项就立即在本文件勾选并新增回归测试。
