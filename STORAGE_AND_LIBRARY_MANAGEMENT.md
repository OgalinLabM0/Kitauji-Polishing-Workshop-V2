# 书架数据、文件位置与缓存

最后更新：2026-08-27（里程碑 027 / SQLite schema v7）

## 1. 三类数据不能混为一谈

1. **原始文件**：用户磁盘上的 EPUB/TXT。导入、删除项目、清空书架和清理缓存都不会修改或删除它。
2. **项目数据库**：应用私有 SQLite，保存原书不可变快照、解析结构、阅读位置、校改草稿、任务断点，以及 schema v7 的实体、别名、证据、时态声明、定向事件、角色知识、世界状态、冲突和译文依赖。删除项目或清空书架会删除这里对应的数据，因此必须显式确认。
3. **派生缓存**：可重建的章节分页 JSON，只用于加快重复打开章节。它不是事实来源，丢失或清空后可从 SQLite 重新生成。

## 2. 删除与清空

- 每部作品行提供独立删除按钮；确认框显示真实书名，并明确原 EPUB/TXT 不受影响。
- “清空书架”要求用户输入“清空”后才可执行，避免一次误触删除全部 V2 项目数据。
- SQLite 依赖外键级联删除章节、正文、阅读位置和校改草稿；当前作品被删除后，自动切换到剩余项目，书架为空时清除当前项目状态。
- 项目删除或清空成功后，同时失效对应派生缓存。
- 当前删除属于永久删除 V2 项目数据；删除前若需可恢复，应先在设置页导出整库备份。尚未实现项目级回收站或单项目归档，确认框不能把删除写成“可撤销”。

## 3. 文件位置设置

“设置 → 文件与缓存”提供三个独立位置：

- 默认书籍目录：导入 EPUB/TXT 的系统窗口优先从这里打开；不固定时使用系统默认位置。
- 默认导出目录：校样和后续成品的另存窗口优先从这里打开；不固定时跟随原书目录。
- 正文缓存目录：用户选中的目录下始终创建专属 `北宇治润色工坊缓存` 子目录，避免把清理范围指向用户任意文件夹。

路径偏好写入应用私有 `storage-preferences.json`，使用临时文件加替换方式保存。界面显示完整路径并允许换行，不隐藏末尾。

项目数据库支持安全迁移到自定义目录。迁移在重启阶段完成，先建立一致副本并核对完整性、schema 和项目数量；目标存在同名数据库时拒绝覆盖，失败继续使用原位置。

## 4. 整库备份、恢复与升级保护

- 设置页可导出完整 SQLite 备份，内容包括原书快照、术语与证据、长期记忆、译文版本、任务断点、复核和阅读位置；API Key 由系统加密文件单独保存，不进入整库备份。
- 恢复分为“选择并暂存校验”与“重启安装”两阶段。恢复前先自动建立当前库的 pre-restore 安全副本，安装后再次检查完整性、schema 和项目数量。
- 软件拒绝恢复高于当前支持 schema 的数据库、无 `projects` 表的普通 SQLite 或完整性检查失败文件。
- schema 升级前使用 SQLite `VACUUM INTO` 建立一致快照；快照版本校验失败时不执行迁移。

## 5. 缓存安全边界

- 缓存根目录必须存在应用标记文件 `.kitauji-cache`；没有标记时拒绝清理。
- 自定义位置永远落在应用专属子目录内。
- 清理逐项删除缓存内容，保留根目录和标记，不跟随目录符号链接。
- 章节缓存包含项目 ID、章节 ID、分页参数和正文页结果；读取时进行运行时结构校验，无效文件按未命中处理。
- 保存中文草稿、删除项目或清空书架会主动失效相关缓存，防止显示旧内容。

## 6. 当前验收

- 129 项自动测试通过，其中覆盖项目级联删除、全库清空、当前项目状态、存储偏好、专属缓存目录、缓存读写/失效、安全清理、整库备份/恢复和升级前快照。
- 1440 级书架、清空确认框、文件与缓存设置、正文校读和术语页完成截图验收。
- 720×680 的书架与设置页无全局横向溢出；长书名、Windows 路径和操作文字保持可读。

尚未完成：回收站/撤销删除、单项目归档导入导出、书架作者/多条件筛选。
