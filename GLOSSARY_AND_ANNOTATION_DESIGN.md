# Version 2：术语表制作、情境变体与 EPUB 注释设计

最后更新：2026-08-25

本文是术语功能的长期规格。它解决三个容易混淆的问题：哪些内容应该全书统一、哪些称呼必须随原文与场景变化，以及哪些特殊译法值得在 EPUB 中为读者添加注释。

## 1. 功能结论

Version 2 的术语表不是简单的“日文字符串 -> 中文字符串”替换表，而是五类互相关联的数据：

1. **基础条目 GlossaryEntry**：固定人物、动物、地点、组织、活动、物品、能力等专名的实体类型、语义身份与中文基准译名；人物/动物可关联带证据的性别事实，其他实体明确标为不适用。
2. **情境变体 GlossaryVariant**：保存昵称、戏称、敬称、称号、临时代称和身份伪装；带说话人、对象、关系阶段、场景与生效范围。
3. **出现记录 GlossaryOccurrence**：保存术语每一次出现的章节、段落、完整日文语境、对应译后中文、本句实际中文呈现、译文状态、说话方向和场景证据。
4. **注释草稿 EpubAnnotationDraft**：对双关、特殊戏称、称号含义等提出可选读者注释；未经人工批准不能进入 EPUB。
5. **审计结果 GlossaryFinding**：翻译后检查基础专名漂移、变体误用、漏用和无原文依据新增。
6. **读音假设 GlossaryWordplayHypothesis**：保存姓名谐音、误读、双读法和后文回收，关联触发证据、反证、中文方案与读者知识风险。

基础条目负责“同一个专名不要随机换译名”，情境变体负责“同一个人不一定始终被同样称呼”。二者不能合并成一张全局替换表。

术语表也不能代替小说长程记忆。人物状态、剧情事件、关系变化、角色声音、叙述视角、伏笔、秘密和章节情绪进入 `LONG_RANGE_MEMORY_DESIGN.md` 定义的叙事记忆，不得为了方便而塞进 Glossary 的备注字段。

## 2. 绝对约束

- 当前日文没有出现术语、姓名、称呼或后缀时，术语库不得授权模型在中文中补写。
- 人物档案知道性别、人数、身份或关系，不得据此向当前译文添加原文没有的性别词、复数词、称谓或剧情事实。
- 日文明示的姓名、别名、称呼、数量、单复数和其他内容不得因术语统一而被删除。
- 不对最终译文做不带节点和语境的全局字符串替换。
- AI 只能提取和提出译名候选，不能自行把条目标成“已批准”或“已锁定”。
- 人工不能只看“日文词 -> 中文词”作出批准。条目引用的每条代表性证据必须同时具有可定位的完整日文语境、对应译后中文和本句实际采用的中文形式；任一缺失时程序禁用批准和锁定。
- 日中审核语境必须来自同一段落和同一翻译候选，界面明确标记机器草稿、人工修订稿或已批准译文，不能把无关中文例句拼成证据。
- 术语注入只发生在当前翻译分段实际命中相应日文形式时。
- 一个日文表面词若对应不同语义身份，必须拆成不同 sense；不能强迫同形词共享译名。
- 冲突、低置信度或变体范围不匹配时进入人工复核，不允许悄悄退回随机翻译。
- 自动流程优先：普通词自动排除，证据一致的常规专名自动确认，未完成的语义问题先继续模型复核；只有多个文学方案、读者知识风险、用户锁定冲突、身份隐藏或模型反复分歧才强制人工。
- 人物与动物的性别必须来自可定位的全书原文证据；姓名印象不能作为结论。地点、组织、活动、物品和概念标记为“不适用”。性别事实不得授权当前句补写原文没有的代词或称谓。

## 3. 哪些内容进入基础术语表

优先提取：

- 角色姓名、姓氏、名字、稳定假名写法和明确别名；
- 国家、城市、学校、地点和设施；
- 组织、社团、阵营、公司和团体；
- 固有称号、作品内正式职务、活动或制度名称；
- 具有作品内固定含义的物品、能力、种族和世界观概念。

默认排除：

- 只有高词频但不具备专名身份的普通词，例如一般语境中的“春”；
- 整句台词、长描述和只为控制某一句表达而制作的条目；
- 可以合理拆开的复合词，除非完整形式本身就是正式专名；
- 把 `さん`、`ちゃん`、`くん` 等直接焊死在角色基础姓名中的条目；这些形式进入情境变体；
- 缺少可定位原文证据的模型猜测。

词频只用于排序，不能决定是否为术语。只出现一次的关键人物或正式专名仍可以进入候选；出现很多次的普通词仍应排除。

## 4. 从全书原文生成术语表

### 4.1 程序预扫描

先由确定性程序读取全部日文文本节点，保护 EPUB 结构并建立稳定段落 ID。程序收集片假名串、重复名词串、ruby 读音、自我介绍格式、称号格式、已有译名和用户导入词表，形成宽松候选集。

这一步不决定中文译名，也不根据性别自动生成先生、小姐、他或她。

### 4.2 全部语境聚合

对每个候选收集：

- 首次、末次及代表性出现位置；
- 每次出现前后的原文窗口；
- ruby、假名读音和同形写法；
- 可能的说话人、受话人、场景和关系阶段；
- 与人物库、组织库和既有锁定译名的链接；
- 同形异义或多个实体共享表面词的冲突。

不能只给 AI 看第一次出现或随机抽取的一句话。译名提案应覆盖候选在全书中的主要语义簇。

### 4.3 AI 专名提取工位

“专名提取”工位只判断候选是否像专名、属于什么类别、是否需要拆分和有哪些语义身份。输出必须包含证据 ID、置信度和拒绝理由；它不能写最终译名或锁定数据库。

### 4.4 AI 术语译名提案工位

“术语译名提案”工位读取该候选的全部代表性语境、读音、人物身份和已有锁定条目，提出一至三个中文候选，并分别说明：

- 音译、意译或约定译法的依据；
- 是否与同一人物的姓、名、别名一致；
- 是否会误添性别、人数、身份或关系信息；
- 是否需要拆成基础条目与情境变体；
- 是否存在同形异义冲突；
- 哪些位置可能值得 EPUB 注释。

模型输出仍是候选。用户可以修改中文、类别、语义说明、证据范围或直接排除。

### 4.5 冲突合并与人工确认

程序进行精确重复、读音重复、别名关系、同形异义和复合词冲突检查。只有用户点击批准或锁定后，基础译名才可进入翻译上下文；“有冲突”状态不得自动生效。

人工确认采用阻断式审核门：

1. 审核者先选择本次拟确认的中文候选；空候选不能批准。
2. 程序逐一解析条目登记的代表性证据 ID，并确认该证据确实属于当前条目。
3. 每条证据并排或上下显示日文原文与同段译后中文，同时显示章节/段落、原文实际形式、本句实际中文形式、说话人、对象、场景和译文状态。
4. 原文实际形式必须能在所示日文语境中看到；对应中文和本句译法必须已经生成。机器草稿可以供人判断，但必须明确标记为未批准，不能伪装成人工定稿。
5. 只有所有代表性证据都成为完整日中对照时，条目才具备决策资格。若证据一致且无创作分歧，系统自动确认；只有进入人工队列的条目才显示最终选择按钮。

“本句实际中文形式”不强制逐字等于基础候选。例如基础候选可能是“春天”，而具体句子自然译为“春风”；这种差异正是人工必须阅读完整日中语境、而不能只看单词映射的原因。

### 4.6 姓名谐音与误读

姓名读音与普通词、感情表达或后文伏笔形成联系时，不把它降格为普通昵称。预读先记录姓名、读音和所有异常读法，再跨章节检索明确触发台词与后文回收，形成带反证的读音假设。

例如“祁帆”被人物听成“喜欢”：首次自我介绍只能确认姓名和读音；后文出现“听起来像喜欢”的台词才能建立谐音联系；再由后续感情线回收验证这不是偶然。模型应先准备直说谐音、调整译字或添加首次注释等中文方案。只有多个方案都忠实但阅读效果不同，才交给人工选择。

用户修改已锁定译名时，系统不直接全文替换旧中文，而是根据源节点映射找出受影响段落，生成定点重译/复核任务，并使相关缓存失效。

## 5. 基础译名如何保证全书统一

翻译每个分段前，程序只检索该分段日文实际出现的已批准条目，再向模型提供：

- 原文形式；
- 基础中文译名；
- 专名类别和简短语义身份；
- 必要的消歧说明；
- 本分段命中的情境变体；
- 明确声明“术语只用于翻译原文已出现内容，不得据此添加任何词”。

锁定基础译名是强约束，但不是盲目字符串替换。当同形词可能是普通词或另一个实体、复合边界不清、或条目与当前 sense 不一致时，系统先复核语义身份。

翻译后，“术语生效审校”工位与确定性检查器共同核对：

- 原文命中基础专名时，中文是否采用了批准译名；
- 中文是否出现未经批准的随机变体；
- 原文没有该专名时，中文是否无依据新增；
- 原文为别名、戏称或敬称时，是否被基础姓名粗暴抹平；
- 修改术语后，旧缓存是否仍在污染结果。

## 6. 称呼、别名与戏称的情境模型

每个变体至少保存：

- 它属于哪个基础人物或实体；
- 日文实际形式与中文候选形式；
- 类型：别名、昵称、戏称、敬称、称号、临时代称或身份遮蔽；
- `speakerIds` 与 `targetIds`，区分 A -> B 和 B -> A；
- 关系阶段、场景、公开/私下、氛围和听众条件；
- 生效起止段落；
- 原文证据、置信度与人工状态；
- 是否提出 EPUB 注释。

使用顺序：

1. 当前日文必须出现基础形式或已登记变体；没有出现就跳过。
2. 若出现已登记变体，检查说话人、目标、关系阶段、场景和时间范围。
3. 只有一个最具体的已批准变体命中时才使用。
4. 同等具体的规则给出不同中文、或当前场景不满足任何规则时进入复核。
5. 没有变体形式、且基础 sense 明确时才使用基础译名。

例如，`関` 的基础译名可以锁为“关”，但 `関さん`、`関ちゃん` 和拖长音的 `関くーん` 分别保存为带原文证据的变体。关系亲近只是理解条件之一；当前原文没写昵称时，不得仅因“已经熟识”而添加“小关”。

## 7. EPUB 注释设计

### 7.1 什么情况值得注释

可提出注释：

- 中文难以同时保留的双关、谐音或文字游戏；
- 戏称、称号或拖长音在当前场景具有明确表达作用，而直译可能令读者困惑；
- 作品内专门概念第一次出现且短注能减少误读；
- 日中文化差异无法在不增写正文的前提下自然传达。

默认不注释：普通姓名、常规敬称、可以直接在正文自然表达的信息、后文才揭示的关系或身份、AI 对人物心理的推测。

### 7.2 审批与读者知识门

每条注释必须：

- 锚定到具体源节点和中文出现位置；
- 带至少一条原文证据；
- 由用户批准或锁定；
- 在该段落的读者知识边界内安全；
- 不把模型推测写成作者事实；
- 默认只在“首次有意义的出现位置”显示，避免重复打断阅读。

若注释内容可能剧透、证据不足或尚未批准，导出器直接排除。

### 7.3 EPUB 3 表达

EPUB 3 输出优先采用 `epub:type="noteref"` 的正文链接和 `aside epub:type="footnote"` 的注释内容，并保留返回链接。支持弹注的阅读器可以弹出显示；不支持时仍能跳转阅读。EPUB 2 或兼容性较差的阅读器回退为章末注/书末注。

概念结构如下，实际生成时必须复用稳定节点 ID 并通过 EPUB 校验：

```xhtml
<a epub:type="noteref" href="#note-1" id="note-ref-1">[注 1]</a>
<aside epub:type="footnote" id="note-1">
  <p>原文在此故意拖长称呼，译文保留其打趣语气。</p>
  <a href="#note-ref-1">返回正文</a>
</aside>
```

纯中文 EPUB 和日中双语 EPUB 共用同一注释记录；双语版的日文原文不被注释生成器改写。

## 8. 用户界面

“术语与专名工坊”使用列表 + 证据检查器结构：

- 顶部显示候选、已批准/锁定、场景变体和日中成对语境数量；
- 支持按状态、类别、章节、出现次数和置信度筛选；
- 列表显示日文原词、中文基准、类别、出现次数和变体数；
- 右侧检查器完整展示长词、读音、候选译名、语义身份、全部代表性日中对照证据和变体范围；
- 每条审核证据都展示日文原文、译后中文、本句实际译法、章节、说话人、对象、场景和机器/人工状态；宽屏并排，窄屏上下排列，所有长文本完整换行而不使用省略号；
- 审核材料完整度以“日中成对语境 x / y”显示。证据缺失、原词不可见、中文未生成或本句译法未记录时，批准按钮保持禁用并列出原因；
- 自动结果可以抽查和改判；待人工项可以选择、修改、锁定、排除、拆分、合并和标记同形异义；
- 注释区显示正文和脚注预览，并可选择首次出现、指定出现或不导出；
- 所有会改变已翻译内容的操作先展示影响范围，不能静默全文替换。

当前代码已经实现译者工作台式结构验收版本、搜索/状态筛选、条目切换、实体/性别记录、姓名谐音方案、日中证据对照、自动处理去向、术语导入和会话内选择反馈。5 个示例中只有谐音存在多个中文方案的“祁帆/喜欢”要求人工；界面明确标注为示例数据，结论尚未写入 SQLite，也未接通真实作品。

## 9. 导入与导出

### 9.1 当前可导入格式

导入窗口同时支持逐条输入和批量文本/文件，文本按 UTF-8 读取。当前格式如下：

1. 每行一对：`関 => 关`，也接受 `->` 或 `→`；空行和以 `#` 开头的行忽略。
2. TSV：前两列是日文与中文，后续可选列依次为类别、备注、读音；可使用 `source/target/category/note/pronunciation` 或对应中文表头。
3. CSV：`source,target,category,note,pronunciation`；支持双引号字段、字段内逗号和 `""` 转义。没有表头时也按上述列序读取。
4. 简单 JSON 对象：`{"関":"关","祈":"祈"}`。
5. JSON 数组：`[{"src":"関","dst":"关","info":"角色姓氏"}]`；也接受 `source/target` 或 `sourceTerm/canonicalChinese` 字段。
6. Version2 基础交换 JSON：`{"version":2,"entries":[{"sourceTerm":"関","canonicalChinese":"关","category":"character","note":"角色姓氏","pronunciation":"せき"}]}`。

类别可写英文或中文别名，归一为：人物、动物、地点、组织、活动/事件、称号/头衔、物品/道具、能力/技能、种族、概念和未分类。未提供类别时保持“未分类”，不能仅凭词形猜人物或性别。

解析器逐行报告缺少日文、缺少中文、未知类别、CSV 引号错误和 JSON 结构错误。同一批中完全重复的映射自动合并；同一日文对应不同中文会阻止加入。若词条已经存在，同译名跳过，不同译名作为冲突阻止导入，不能静默覆盖用户决定。

### 9.2 导入后的权限

默认选项是“先匹配原文后核对”：条目进入待核对状态，等真实作品扫描后补出现位置、日中语境、实体、性别证据、称呼变体和谐音线索。用户也可以明确选择“作为用户锁定译名”，但锁定的只是日文实际命中时的基础对应，不能授权全局字符串替换、补写原文没有的词或绕过增译/漏译审校。

当前 Version2 基础交换 JSON 只承载平面条目字段，不假装承载完整证据。完整项目级格式仍需在 SQLite schema 稳定后保存 sense、实体/性别证据、变体、谐音、原文位置、注释、审批状态和版本迁移信息。以后导出为简单格式时，软件必须明确提示这些信息会丢失。

当前界面中的导入结果仅在本次会话存在；关闭软件后不会保存。`samples` 目录提供 TXT、CSV 和 JSON 三份可直接测试的示例。

## 10. 独立 AI 工位与权限

术语流程新增三个职责隔离工位：

1. **专名提取**：提候选与证据，不写中文成稿。
2. **术语译名提案**：提中文候选、sense、变体与注释草稿，不锁定。
3. **术语生效审校**：翻译后报告基础专名漂移、变体误用和无依据新增，不直接改最终 EPUB。

它们可以调用同一个 DeepSeek V4 Flash，但每次是独立任务，读取范围和输出 schema 不同。长期状态由项目数据库管理，不由某个工位的聊天记忆管理。

## 11. 当前代码状态

已经完成：

- `GlossaryEntry`、`GlossaryVariant`、`GlossaryOccurrence` 和 `EpubAnnotationDraft` 类型；
- 出现记录中的译后中文、本句实际中文形式、译文状态和翻译候选关联；
- 人物/动物/地点/组织/活动等实体类型、带证据的性别推断和姓名谐音假设；
- 当前原文命中、人工状态、情境变体冲突和注释导出规则；
- 30 项术语规则、导入解析、分段注入、人工审核门、实体性别和谐音路由单元测试；
- 专名提取和术语译名提案的最高忠实提示契约；
- 只打包当前分段实际命中条目、并拦截同形异义冲突的上下文打包器；
- 三个术语相关 AI 工位注册；
- 术语表结构验收界面、搜索、筛选、条目选择、实体/性别、日中证据、姓名谐音方案、自动/模型/人工去向、逐条输入、批量导入、错误定位、预览、会话内反馈和注释预览。

尚未完成：

- EPUB/TXT 实际原文扫描与候选抽取；
- SQLite 术语表、证据和版本迁移；
- DeepSeek API 请求与结构化输出解析；
- 审核结论与导入词条的 SQLite 持久化、真实编辑/锁定、影响范围计算和完整项目级导入导出；
- 把批准条目注入翻译任务；
- 向成品 EPUB 写入并验证脚注。

这些未完成项必须在后续阶段逐项测试，不能因为界面存在就宣称真实 AI 术语表已经可用。

## 12. 参考与独立实现边界

- LinguaGacha 的公开术语说明强调术语表用于人名、地名、组织名等反复专名的一致性，不宜控制普通词、称呼或整句，并要求人工复核自动结果。Version 2 采用这些边界，同时增加 sense、证据、方向性情境变体和读者知识门：<https://github.com/neavo/LinguaGacha/wiki/Glossary>
- AutoNovel 的翻译包把 glossary 与前文、历史一起放进分段上下文，并在构造提示时只选择当前分段出现的术语。Version 2 采用“按分段命中注入”的类别思路，但独立实现更严格的数据和审计模型：<https://raw.githubusercontent.com/auto-novel/auto-novel/main/packages/translator/src/types.ts>、<https://github.com/auto-novel/auto-novel/blob/main/packages/translator/src/translator/openai-prompt.ts>
- EPUB 3 使用结构语义支持注释和弹出脚注行为；具体打包实现以当前 W3C EPUB 规范和校验结果为准：<https://www.w3.org/TR/epub-33/>、<https://www.w3.org/TR/epub-overview-33/>

Version 2 不复制上述项目源码。AutoNovel 使用 GPL-3.0；在 Version 2 许可证尚未决定前，仅参考公开行为和抽象设计，所有代码与数据模型独立编写。
