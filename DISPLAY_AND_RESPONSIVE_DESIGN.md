# 显示设置与响应式窗口设计

## 1. 目标

Version 2 是长时间阅读和审校工具，不能假设所有用户都使用同一字号、同一系统缩放或 1440 像素以上窗口。显示系统必须同时满足：

- 用户可以直接调整全软件界面文字；
- 修改立即生效，关闭并重新打开后保持；
- 放大文字不依赖浏览器整页缩放，不把可用窗口宽度伪装成另一个尺寸；
- 窗口变窄时重排信息，不用省略号隐藏长书名、路径、原文、译文或证据；
- 状态标签保持完整，正文和说明允许自然换行；
- 起始页的京吹风格动画与视觉语言继续保留。

## 2. 当前实现

“设置 -> 显示”提供 85%–140% 的五个百分点调整、90%/100%/115%/130% 常用档位、加减按钮和实时日中预览。快捷键为：

- `Ctrl + +`：放大；
- `Ctrl + -`：缩小；
- `Ctrl + 0`：恢复 100%。

设置保存到应用本地存储键 `kitauji-v2.display-settings`。读取时进行类型、有限数值、上下限和步进归一化；损坏或旧格式数据回退到 100%，不能阻止软件启动。正式项目 SQLite 接入后，显示偏好仍属于应用级设置，不随某一本作品改变。

## 3. 字号缩放策略

界面根节点维护无单位变量 `--text-scale`。工作台、术语表、章节阅读器、校改输入、导入窗口、设置页和起始页中全部显式像素字号都通过该变量计算；没有单独字号的按钮和表单控件继承已缩放的界面字号。

不采用 `transform: scale()`：它只缩放绘制结果，容易产生模糊、点击区域错位和被裁切内容。不把 Electron 页面缩放作为字体设置：页面缩放会同时改变布局坐标与窗口断点，用户难以判断到底是字体变大还是窗口变小。当前方案让文字真实参与布局计算，迫使表格、按钮和检查器按实际占用空间重排。

里程碑 012 根据实屏反馈重新校准默认字号：应用最左侧主导航维持既有层级，不额外放大；书架正文、页首说明、工具栏、章节目录、校读元数据、术语表/证据检查器和设置表单等主工作区提高操作文字下限。路径可以略小但必须完整换行；日文/中文正文继续使用独立阅读字号，不能因界面操作字号调整而被压回表格密度。

页面也不再套用同一种“标题加卡片”节奏：书架使用连续目录式列表，作品主页使用编辑总览，正文校读使用章节轨道加安静纸面，术语使用列表加证据检查器，设置使用分类索引加文档式选项。各页只用克制的章节强调色区分，不依赖渐变、大面积彩色卡片或装饰图标制造层次。

## 4. 窗口断点

Electron 正式窗口最小尺寸从 1080×700 调整为 720×600。布局使用窗口内逻辑像素，因此 Windows 系统缩放变化仍会进入同一响应式规则。

- 1260 以下：术语列表与证据检查器由左右并列改为上下排列。
- 1180 以下：作品总览的流程表与事实检查器改为单列。
- 980 以下：显示设置的字号面板与窗口说明改为单列。
- 900 以下：术语页标题、工具栏和导入窗口底部操作按可用空间堆叠。
- 760 以下：主侧栏收为图标；模型状态隐藏；工位职责移到第二行；术语表改为紧凑双行记录；导入日中输入改为上下排列；章节目录从左侧轨道改为正文上方滚动区。
- 720 高度以下：起始页缩小徽章和纵向间距，保留进入按钮与动画，不让主要操作被窗口底部裁切。

这些断点只决定结构，不允许对书名、文件路径、日文原文、中文译文、术语说明或错误信息使用单行截断。

## 5. 当前验证矩阵

- 1440×900，100%：设置页双栏、完整侧栏、预览和控制条。
- 1440×900，关闭后重开：130% 设置成功恢复。
- 720×680，130%：侧栏图标化，设置页单列，字号控制与窗口说明可纵向滚动。
- 720×680，130%：作品总览长中日混合书名和 Windows 路径完整换行；工位表变为双行。
- 720×680，130%：术语列表变为紧凑双行，状态标签不拆分，长专名与说明继续换行。
- 720×680，130%：逐条导入的日文与中文输入上下排列，类别/读音/备注和固定操作区可滚动到达。
- 720×680，140%：章节目录位于上方，当前阅读项可见；超长日文书名和 Windows 路径完整换行；正文与校改操作无全局横向溢出。内容视口 706×643，文档宽度同为 706。
- 1426×779，100%：里程碑 012 的书架、清空确认框、文件与缓存设置、正文校读和术语证据页完成实屏复查；主工作区小字层级已提高。
- 706×643，100%：新版书架搜索/排序/删除动作和文件与缓存设置自动改为图标轨道、上下操作区和单列文档，页面宽度与视口同为 706，没有全局横向溢出。

## 6. 后续验收

正式发布前仍需在 Windows 100%、125%、150%、175% 系统缩放，以及多显示器不同缩放切换时进行实机测试。人物关系图、复核队列和后续双版本翻译编辑器接入时必须沿用同一字号变量与断点纪律，并分别加入最窄窗口、最大字号和最长真实内容截图回归。
