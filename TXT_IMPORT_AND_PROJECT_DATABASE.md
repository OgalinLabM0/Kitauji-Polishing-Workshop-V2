# 日文 TXT 导入与项目数据库

最后更新：2026-08-26

## 1. 本里程碑的完成边界

本文记录 TXT 源文层：用户在系统文件窗口中选择日文 TXT，软件保存未经改写的原始文件和完整解码文本，建立章节/段落索引，并在重启后恢复作品。009 已另行接入 EPUB，只读结构见 `EPUB_IMPORT_COMPATIBILITY.md`。两条导入路径都没有调用 AI，也没有生成译文、人物结论或术语。

## 2. 文件与权限边界

- Renderer 不接收任意文件系统或 SQLite 权限，只能调用列出项目、读取当前项目、打开既有项目、请求系统选择并导入 TXT。
- 文件选择框由 Electron 主进程打开，Renderer 不能提交一个自造路径让主进程读取。
- 主进程只接受绝对 `.txt` 文件，检查是否为普通文件，并把单文件上限限制为 64 MiB。
- 原文件只读，不会被改名、移动或写回。数据库保存在 Electron 的应用数据目录；隐藏验收参数可以把测试库定向到项目内 `artifacts`，正式界面不暴露这个能力。

## 3. 解码策略

按以下顺序处理：UTF-8 BOM、UTF-16LE BOM、UTF-16BE BOM、无 BOM 的严格 UTF-8；只有 UTF-8 严格解码失败时才尝试 Shift_JIS。

解码使用 `fatal` 模式，不用替换字符掩盖坏字节。空内容、二进制空字符和控制字符比例异常会阻断导入。数据库记录实际编码与 CRLF/LF/CR/混合换行类型；原始字节和完整解码文本均保留，因此后续解析器升级时可以从源文重建索引。

## 4. 章节与段落

章节标题只识别有限、明确的形式，例如 `第一章`、`序章`、`プロローグ`、`終章`、`幕間` 和一级至三级 Markdown 标题。标题前存在书名或作者信息时保存为“开篇”；完全没有标题时整份文本保存为“正文”。宁可少切分，也不把正文句误判成章节。

每个非空源行当前形成一个稳定段落，记录章节内顺序与原文件行号。判断空行时会检查空白，但非空行保存原字符串，不裁掉行首缩进、半角空格或全角空格。章节内容和完整解码文本另行保存；标题没有被计入界面“正文字符”指标，原文仍存在于完整文本与章节标题字段中。

## 5. SQLite v3 中保留的 TXT 源文表

当前 schema 包含：`projects`（作品身份、原路径、编码、SHA-256 和统计）、`source_documents`（原始 BLOB、完整解码文本和换行）、`chapters`、`paragraphs` 与 `app_state`。

008 首次使用 SQLite v1；009 升级为 v2 并保留原有 TXT 表。旧项目原地迁移后经过外键检查。数据库开启外键和 5 秒 busy timeout，文件数据库使用 WAL 与 `synchronous=NORMAL`。新项目的作品、源文、章节、段落和当前状态必须在一个 `BEGIN IMMEDIATE` 事务中全部成功，否则回滚。相同 SHA-256 的源文件更新可见路径和最近打开时间，但保留最初保存的源文快照，不复制内容。

当前 `node:sqlite` 在所用 Node 22 类型中仍会显示实验性警告；已锁定 SQLite 文件格式和显式迁移版本，后续如改用稳定驱动，必须以现有数据库往返回归为前提。

## 6. 界面与验证

作品总览显示真实书名、可完整换行的绝对路径、格式、编码、快照状态、章节/段落/字符/大小和章节目录。项目为空时不再显示伪造路径。窗口最小 720×600，应用字号最大 140%。

- 单元测试覆盖 UTF-8/UTF-16/Shift_JIS、空/二进制拒绝、保守标题、开篇、无标题正文、源行、原始空白、事务写入、原字节读取、SHA-256 去重和真实样例导入。
- 真实 Electron 首次导入后数据库为 1 个项目、4 章、7 个正文段落、575 字节原始快照，`PRAGMA integrity_check` 为 `ok`。
- 关闭后使用同一数据库重新启动，恢复截图与首次导入后的截图 SHA-256 完全相同。
- 720 像素窗口、140% 应用字号的布局报告没有全局横向溢出。

## 7. 后续接入

下一层应在 v2+ 迁移中加入结构预检结果、人物/关系/场景/证据、术语 occurrence、译文版本、审校发现和任务状态。EPUB 必须走 container/OPF/spine/XHTML 的安全 AST 适配器，但最终写入同一项目领域，不另造一套不兼容记忆。正式备份/恢复、项目改名/删除、章节规则人工修正和源文件变更检测也仍待建设。
