# Version 2：自动审校、仲裁与最小人工复核

最后更新：2026-08-26

## 1. 结论

Version 2 不会把后续审校简化成一个 Judge，也不会把所有不确定项都推给用户。目标结构是：

1. 确定性检查负责能够精确验证的红线；
2. 专长审校工位分别检查忠实、人物关系、称呼、术语、角色声音和全书轨迹；
3. 自动修复器只修复已经定位的问题，修复后重新跑完整质量门；
4. 独立复核解决普通语义分歧；
5. 人工只处理机器无权或无法可靠决定的文学选择。

任何 Judge 都没有权力跳过增译、漏译、内容净化、无依据强化、数字事实、源节点对应和 EPUB 结构完整性。模型可以指出程序误报，但必须提交原文证据并由对应规则重新验证，不能用一句“文学上合理”直接特赦。

## 2. 旧版实际具有的审校层次

已核对外侧旧版 `北宇治润色工坊/services/deepseekService.ts`、`constants.ts` 和帮助页。旧版不是单层审校，主要流程包括：

### 2.1 逐段程序校验

- 空结果；
- 未翻译假名泄漏；
- 乱码是否原样保留；
- 非原文口吃造成的重复字；
- AI 服务用语污染；
- 相对 Sakura 中文的长度剧变；
- 对话引号增删；
- 第一、第二人称是否被删除；
- 原文没有 `彼/彼女` 时中文是否出现“他/她”；
- 数字是否仍能匹配；
- 术语否决标记、智能模式术语遗漏和严格模式术语遗漏。

### 2.2 自动自愈阶梯

旧版对失败结果依次尝试：

1. 主模型带错误原因引导重试，两次；
2. Judge 分析失败并给出修正指令，再由主模型重试，两次；
3. Judge 直接编辑译文，两次；
4. 最终仲裁决定校验器是否误报；
5. 批处理失败时使用拆分/回滚策略缩小问题范围。

### 2.3 章节级回查

章节完成后，批量 Judge 还会扫描：

- 特殊语癖；
- 同句第一人称切换；
- 中文复数而日文无明显复数标志；
- 中文性别代词而日文没有 `彼/彼女`；
- `ちゃん/さん` 的固定后缀是否遗漏或变成先生、女士、小姐；
- 后续原文提供的新线索。

这些能力不能在 Version 2 中丢失。新版会把它们拆成可测试的检查器、专长工位和路由，而不是继续集中在单个服务文件和单个“终极判官”提示中。

## 3. 旧版不能原样继承的部分

旧版层次丰富，但存在会破坏最高忠实规则的问题：

- Judge 直接编辑后可以设置 `skipPronounCheck: true`，跳过代词删除和性别臆测两类检查。
- 最终仲裁提示允许“只要文学性、流畅度或逻辑上合理，即便违反机械规则也通过”，范围过宽。
- 数字格式、引号和术语错误可以被最终 Judge 特赦，与提示词中的“数字形式严格继承”等规则互相冲突。
- `さん -> 桑`、`ちゃん -> 酱` 被写成固定全局规则，无法处理作品风格、正式场合、中文本土化和人物关系变化；Version 2 改为原文明示形式加情境译法。
- 复数、代词、长度和口吃主要靠正则启发，存在误报与漏报；长度还以 Sakura 中文为参照，不是直接比较日文信息集合。
- 校验函数遇到第一项错误就返回，不能一次形成完整问题清单。
- 同一 Judge 同时诊断、指导、直接改稿和终审，职责与权限没有隔离。
- 对 `ライン/莱茵` 等已知案例存在硬编码“思想钢印”，能救单个案例，却不能形成可泛化的 sense 与证据模型。

Version 2 保留旧版的多轮自愈和后文回查，但取消 Judge Supremacy。最高权限属于原文证据、用户锁定决定和确定性结构门。

## 4. Version 2 审校流水线

### 4.1 第一层：确定性红线

一次收集全部问题，不在第一项错误处停止：

- 源段落和译文候选是否一一对应；
- 空结果、非法字符、未翻译文本和污染文本；
- 数字值、否定、比较级、专名、明确单复数和明确人称是否丢失或反转；
- 引号、ruby、脚注、链接、图片、受保护子树和 EPUB 节点是否保持；
- 内容尺度是否被净化、弱化或反向加码；
- 当前日文没有相应信息时，是否新增性别词、人数、称呼、动作、心理和剧情事实。

硬规则失败后只能自动修复或阻断。任何模型修改后的文本必须重新通过全部硬规则，不能沿用旧版的跳过参数。

### 4.2 第二层：局部专长审校

- 忠实审校：漏译、增译、错译、否定、主客体、动作链、尺度变化；
- 场景/关系审校：说话人、受话人、称呼方向、关系阶段、公开/私下和气氛；
- 术语审校：sense、基础专名、别名、读音、同形异义和当前段实际译法；
- 角色声音审校：第一人称、敬语、口癖、粗俗度和情绪，但不能改动信息集合；
- 结构审校：EPUB 精确节点和不透明行内标记。

每个工位只提交问题、证据和修复候选，不能写最终 EPUB。

### 4.3 第三层：自动修复

可稳定定位的问题自动处理，例如：

- 未翻译片段和模型前缀污染；
- 已确认专名在单一 sense 中发生漂移；
- 已确定的引号/标记映射遗漏；
- 译文无依据添加性别代词或复数词；
- 漏掉原文明示的数字、否定或称呼形式。

修复器只收到当前问题、源证据和允许修改的文本槽位。修复完成后重新跑第一至第二层；连续失败进入专长复核，不直接交人工。

### 4.4 第四层：独立语义复核

多义词、主语延续、隐含宾语、场景语气等无法完全靠程序判断的问题，由两个独立任务读取相同原文证据但分别作答。它们可以使用同一个 DeepSeek V4 Flash 模型，但提示契约、随机种子/采样和任务上下文彼此隔离。

两个高置信结论一致时自动通过或自动修复；证据不足时由专长工位继续查全书。模型投票不能覆盖硬规则，也不能把多数意见冒充原文事实。

### 4.5 第五层：全章和全书轨迹回查

- 后文揭示触发相关前文的定点复核；
- 人物声音、称呼和关系变化按剧情时间检查；
- 同一专名的 sense、译名和特殊读法全书核对；
- 伏笔、身份隐藏和读者知识边界检查；
- 修改一个锁定条目后，只重查受影响的源节点。

这继承旧版“章节结束后再回头修”的优点，但依赖结构化事件和证据，不只看固定数量的后续句子。

## 5. 人工队列只保留什么

默认不要求人工逐条批准 AI 术语或每段译文。以下情况才强制人工：

1. 多个方案都忠实，但文学效果明显不同，例如谐音姓名究竟直写、改译还是加注；
2. 处理方式可能提前泄露身份、感情线、伏笔或后文秘密；
3. 自动建议与用户锁定规则冲突，软件无权替用户改写；
4. 两次独立复核和补证据流程仍互相矛盾；
5. 作者正在刻意隐藏身份或性别，而中文是否显化会改变阅读体验。

以下情况不应占用人工：

- 普通词误入术语表；
- 证据充分且无 sense 冲突的常规人名、地名、组织名；
- 可以确定性修复的格式、污染、遗漏和结构错误；
- 模型还没有完成证据检索的中间状态；
- 系统自身没有跑完检查。这种情况应阻断并重跑，不应让用户替软件兜底。

用户仍可抽查、改判和提高抽样比例，但这属于质量监督，不是完成任务的必需劳动。

## 6. 姓名谐音、误读与双关

“祁帆被听成喜欢”不能只保存为昵称变体。新版使用 `GlossaryWordplayHypothesis`：

- 姓名原形、读音和假名/汉字形式；
- 被听成或故意读成的另一个词；
- 首次自我介绍、触发误读的台词和后文回收事件；
- 反证与其他可能解释；
- 中文候选：直说谐音、调整译字、正文重构或首次注释；
- 是否会剧透、是否需要读者注释；
- 置信度、状态和完整证据 ID。

自动流程：

1. 全书预读记录姓名、ruby、假名读音和所有异常称呼；
2. 读音工位生成同音/近音候选；
3. 剧情线索工位检索“读作、听起来、名字、喜欢、叫错”等元语言台词和后文回收；
4. 独立审校检查该联系是否只是巧合，并主动寻找反证；
5. 只有一个安全中文方案时自动采用；存在多个文学方案或读者知识风险时，连同已整理好的方案交给人工选择。

人工不需要自己从几十万字里发现双关；软件必须先把假设、证据、后文回收和可选译法准备完整。

## 7. 术语中的实体类型与性别

每个词条新增实体类型：人物、动物、地点、组织、活动、物品、能力、种族、概念、称号或其他。

- 人物和动物可以记录男性、女性、非二元/作品自定义或未知；
- 地点、组织、活动、物品和概念标为“不适用”，不做性别推断；
- 姓名、立绘印象和常见性别刻板印象不能成为确认依据；
- 确认性别必须有全书原文证据、证据 ID、置信度和读者首次得知位置；
- 证据不足时保持未知，不为了填满字段而猜测。

人物库知道性别只用于消歧、关系分析和发现前后矛盾。若当前日文没有性别表达，该事实仍不能授权中文添加“他/她、先生/小姐”等信息。

## 8. 当前代码状态

已经完成：

- 通用复核问题、独立复核票、人工专属原因和五种处理去向的数据模型；
- 确定性路由器：自动通过、自动修复、专长复核、人工必需或阻断；
- 硬规则不可被 Judge 特赦、修复后必须完整重验的代码边界；
- 实体类型和带证据的性别记录；
- 姓名谐音/双关假设、证据和中文方案模型；
- 12 项新增单元测试，覆盖自动路由、最小人工、性别证据和谐音处理；
- 术语界面示例：5 个条目中只有“祁帆/喜欢”进入人工，普通词自动排除，其余自动确认或继续模型复核。

尚未完成：

- 旧版全部程序检查器的 Version 2 实际迁移；
- DeepSeek 请求、独立复核任务、修复循环、章末/全书扫描和失败预算；
- SQLite 复核记录、问题聚合、自动结论抽样和用户改判；
- 真实 EPUB/TXT 上的谐音检索、读音索引和后文回收；
- 可用的完整复核队列页面。

因此当前已经建立了新版不会退化成“一个简陋 Judge”的架构和可测试路由，但尚不能宣称真实整书审校已经接通。
