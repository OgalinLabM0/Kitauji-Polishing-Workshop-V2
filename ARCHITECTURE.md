# 北宇治润色工坊 Version 2：软件架构

最后更新：2026-08-26

## 1. 结论：不是一个“大脑”管理全部内容

Version 2 采用“确定性流程控制器 + 职责隔离的 AI 工位 + 统一作品事实库”。所有 AI 工位可以调用同一个 DeepSeek V4 Flash，但它们不是一个不断膨胀的全局会话：每个工位拥有独立任务契约、最小相关上下文、允许读取的数据范围和允许提交的候选结果。

作品事实库是唯一长期状态来源。AI 只能提出带证据的候选更新，流程控制器完成格式校验、权限检查和冲突检测后才能入库；用户锁定内容不能被 AI 覆盖。

## 2. 桌面进程结构

首发桌面壳确定为 Electron，前端继续使用 React + TypeScript。

### 界面进程 Renderer

- 显示书架、京吹风格起始页、翻译工作台、作品认知中心和复核队列。
- 不直接访问文件系统、SQLite、系统密钥或任意 Node.js API。
- EPUB 阅读内容在隔离、清洗后的阅读视图中呈现。

### 桌面主进程 Main

- 管理窗口、原生文件选择、应用生命周期和受限 IPC。
- 默认开启上下文隔离与渲染沙箱，拒绝任意导航和新窗口。
- 只暴露经过 schema 校验的窄接口，不向界面暴露通用文件系统能力。

### 后台工作进程 Worker

- EPUB 解包、解析、结构验证和重新打包。
- SQLite 读写、迁移、备份和影响范围查询。
- AI 请求队列、暂停、恢复、取消、限流和费用统计。
- CPU 密集或不可信内容处理与界面进程隔离，单个任务崩溃不能拖垮界面。

## 3. AI 工位

| 工位 | 读取内容 | 允许提交 | 禁止事项 |
| --- | --- | --- | --- |
| 全书预读 | 章节原文、既有事实 | 人物、事件、关系、证据候选 | 生成最终译文 |
| 专名提取 | 全书原文、已有条目、出现语境 | 专名、sense、拆分与排除候选 | 决定最终中文或锁定条目 |
| 术语译名提案 | 候选的全书语境、读音、人物与知识边界 | 中文候选、情境变体、注释草稿 | 把候选直接注入翻译 |
| 场景分析 | 当前场景、邻近场景、相关记忆 | 场景边界、说话人/受话人候选 | 改写原文或译文 |
| 忠实初译 | 当前日文、局部窗口、相关快照 | 中文草稿、逐项源文覆盖审计 | 增译、漏译、净化、弱化或无依据强化 |
| 中文编辑 | 日文、草稿、角色声音 | 不改变信息集合的中文改写 | 新增/删除事实 |
| 忠实审校 | 日文、候选译文、结构化审计 | 问题清单与证据 | 直接放宽最高忠实规则 |
| 关系称呼审校 | 原文明示称呼、关系轨迹、场景 | 称呼误判和漂移问题 | 在原文无称呼时补写称呼 |
| 术语生效审校 | 原文、已批准条目、出现记录、候选译文 | 漂移、误用、漏用和无依据新增问题 | 全书字符串替换 |
| 全书轨迹审校 | 全书译文、事件流、人物声音 | 定点回修任务 | 全书无差别替换 |

AI 工位的输出默认是候选，不直接写入最终 EPUB。程序校验器负责数字、性别词、单复数、称呼、内容尺度、引号、乱码、源译映射和结构完整性；不能用多个 AI 工位投票取代原文证据。旧版 Judge 的诊断、指导重试和章末回查会迁移为职责隔离任务，但任何 Judge 都不能跳过硬规则；修复稿必须重新通过完整质量门。复核路由与最小人工条件见 [REVIEW_AND_ADJUDICATION_DESIGN.md](./REVIEW_AND_ADJUDICATION_DESIGN.md)。

## 4. 作品事实库

当前已落地源文与校样层 SQLite v3。TXT 使用 `source_documents`、`chapters` 和 `paragraphs`；EPUB 使用 `source_archives`、`epub_documents`、`epub_spine_items` 和 `epub_text_blocks`；两者共用 `projects`、`app_state` 与 `reading_positions`，双语普通中文段的人工校改保存在 `epub_block_drafts`。导入在单个写事务中保存 SHA-256、原文件快照、结构、稳定位置和可见文本；草稿不覆盖不可变源表。相同源文按指纹复用项目，应用启动后恢复上次打开项。v1 TXT 项目可顺序迁移到 v3，v2 EPUB 项目可原地迁移并保留结构。人物、关系、场景、证据、正式译文版本、任务与审计表仍属于后续迁移，不能把当前源文库称为已经完成的全书认知库。

当前项目库由 Electron 主进程拥有。Renderer 只能调用项目列表/打开/删除/清空、系统选择器导入、分页正文读取、受控草稿保存、阅读位置和系统另存导出等窄接口，不能自行提交输入或输出文件路径，也不能获得通用文件系统或 SQL 接口。删除和清空只作用于 V2 的 SQLite 项目；原始 EPUB/TXT 不在删除目标内。正文界面只接收文本与元数据，不接收可执行 XHTML。当前解析耗时在四本实测样书上为 0.6–3.7 秒；后续更大 EPUB、模型预读和大规模分析迁到 Worker 时，界面权限边界保持不变。

章节分页结果另存为可重建 JSON 缓存，不是作品事实库。缓存路径由主进程管理，必须带应用标记并放在专属子目录；保存草稿、删除项目或清空书架会失效相关缓存。默认导入目录、默认导出目录与缓存目录分别配置，Renderer 只能请求系统目录选择器，不能把任意路径作为清理目标。完整边界见 [STORAGE_AND_LIBRARY_MANAGEMENT.md](./STORAGE_AND_LIBRARY_MANAGEMENT.md)。

每本书拥有独立数据库，至少包含：

- 原始资源、章节、场景、段落和稳定源节点 ID；
- 人物、别名、性别、人数、身份、说话风格及证据；
- 有向关系事件流和任意剧情位置的关系快照；
- 剧情事件、人物状态、角色声音、叙述视角、地点别名、双关决策、章节情绪、伏笔、秘密和未决问题；
- 原文明示称呼及中文映射，不保存一个永远不变的 A -> B 称呼；
- 译者知识、当前读者知识和角色知识三层边界；
- 基础术语、语义身份、全部出现位置、情境变体、EPUB 注释草稿、用户锁定决定、提示版本、模型版本和审计记录；
- 初译、编辑稿、审校稿、人工稿及其来源关系；
- 任务、重试、费用、失败、暂停和恢复状态。

## 5. 主流程

1. 导入 EPUB/TXT，建立不可变源文快照和节点映射。
2. 全书或逐章预读，建立带证据的作品认知。
3. 扫描专名、聚合全书出现语境、生成译名候选，由用户批准或锁定基础条目与情境变体。
4. 按场景顺序分析对话、人物、关系和知识边界。
5. 忠实初译，只注入当前日文实际命中的已批准术语，并逐项证明译文内容来自何处。
6. 中文编辑，只改善表达，不改变信息集合。
7. 程序校验与 AI 定点审校；术语漂移、变体冲突和其他争议进入复核队列。
8. 后文揭示触发受影响前文的定点回查，不全书重做。
9. 通过全部质量门后，以源 EPUB 为骨架生成纯中文或日中双语 EPUB；已批准且不剧透的注释按稳定节点写入。

长期记忆不会作为一段无结构摘要塞进模型。取回器按场景、A -> B 关系方向、术语、章节、人物和有效时间筛选，并将结果拆成读者安全、译者专用、角色知识和未决项。详见 [LONG_RANGE_MEMORY_DESIGN.md](./LONG_RANGE_MEMORY_DESIGN.md)。

EPUB 正式导出使用稳定节点路径、源摘要、`text`/`tail` 槽和复杂块不透明标记；定位或结构验证失败时阻断导出，不允许静默扁平化。详见 [EPUB_PROCESSING_DESIGN.md](./EPUB_PROCESSING_DESIGN.md)。

同一场景内必须保序处理；互不依赖的章节资源检查、图片处理和确定性校验可以并行。并发由流程控制器决定，AI 不能自行派生无限任务。

## 6. 起始页与工作台

### 起始页：受保护兼容资产

完整保留旧版京吹风格语言：五线谱背景、制服栗色、铜管金、徽章浮动、空气粒子、按钮流光、日文竖排装饰和 0.8 秒模糊退场。远程 Logo 与在线字体改为本地资源，保证离线可用；尊重系统“减少动态效果”设置。

### 工作台：内容优先

- 长书名、章节名和 Windows 路径独占可换行区域，不用省略号隐藏关键信息。
- 章节、段落、人物关系、证据和任务使用稳定的表格、轨道和检查器布局。
- 主要阅读文字可使用克制的衬线字体；操作、状态和元数据使用清晰的无衬线字体。
- 人物、关系阶段、置信度和任务状态使用不拆行的原子标签。
- 窄窗口优先堆叠信息，不压缩到无法阅读。
- 术语工坊采用列表 + 证据检查器，基础译名、情境变体和读者注释分区呈现，不把长原文证据塞进单行标签。
- 全部操作文字使用统一 `--text-scale`，由显示设置在 85%–140% 调整；字体真实参与布局计算，不使用会造成裁切和点击错位的画布缩放。
- 正式窗口最小为 720×600；侧栏、总览、术语表、导入窗口和设置页分别按内容宽度进入单列或双行结构。详细断点与验证见 `DISPLAY_AND_RESPONSIVE_DESIGN.md`。
- 章节阅读器使用目录轨道 + 正文纸面的结构；窄窗口把目录变为上方滚动区，当前章节自动进入视野，原文和校改稿不以聊天气泡呈现。
- 应用主导航保持稳定尺寸；书架正文、章节轨道、工具栏、术语证据、表格和设置表单提高操作字号下限。书架、作品主页、校读、术语和设置使用不同的空间结构与克制色调，不复制同一套卡片墙。

## 7. 第一阶段建设范围

- Electron 安全桌面壳与 React/Vite 工程。
- 本地化京吹风格起始页。
- 书架/工作台基础导航和真实长内容响应式布局。
- 领域模型、AI 工位注册表和确定性任务状态机。
- 最高忠实规则的程序常量与首批单元测试。
- 术语领域模型、三项职责隔离工位、规则测试和结构验收界面。
- 反净化提示契约、内容尺度审计、长程记忆取回契约和 EPUB 精确写回契约。

本阶段不声称已经实现 EPUB 翻译或达到人类译者质量；后续按 `PROJECT_BRIEF.md` 的阶段清单逐项完成并验收。

术语提取、情境变体、人工确认、按分段注入与 EPUB 注释的完整规格见 [GLOSSARY_AND_ANNOTATION_DESIGN.md](./GLOSSARY_AND_ANNOTATION_DESIGN.md)。
